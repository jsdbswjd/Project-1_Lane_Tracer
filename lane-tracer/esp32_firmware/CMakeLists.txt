# Top-level CMakeLists for ESP-IDF project.
# Run `idf.py build` from this directory.

cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(lane_tracer_firmware)
